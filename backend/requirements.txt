# Production dependencies only. Keep dev/test/CLI tooling in requirements-dev.txt.
fastapi==0.139.0
uvicorn[standard]==0.50.0
python-dotenv==1.2.2
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.14.2

# Supabase/Postgres and auth
supabase==2.31.0
PyJWT==2.13.0

# PHI Scrubbing (regex-only mode — no spaCy needed)
# Uses Presidio's built-in pattern recognizers with NoOpNlpEngine
# to avoid the ~100MB spaCy + en_core_web_sm slug bloat on Heroku.
presidio-analyzer==2.2.353
presidio-anonymizer==2.2.353


# LLM orchestration and providers
langchain-core==1.4.9
langchain-google-genai==4.2.6
langchain-groq==1.1.3
langchain-openai==1.3.3
langchain-text-splitters
langgraph==1.2.7
google-genai==2.10.0
groq==0.37.1
openai==2.44.0
tiktoken==0.13.0
tenacity==9.1.4
langsmith>=0.3.45

# HTTP and document processing
httpx==0.28.1
requests==2.34.2
pymupdf==1.28.0
pymupdf4llm==1.28.0
pillow==12.3.0
# NOTE: pdf2image and pytesseract have been removed (Phase 2 upgrade).
# All document OCR is now handled by Gemini Multimodal API (pdf_extractor.py).
# This eliminates column-mangling issues in multi-column EOB financial tables.

# Optional outbound email integration
resend==2.32.2
